-- =============================================
-- DAYI KEY SYSTEM + Ses + Close Button
-- =============================================
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

local validKeys = {
    "DAYI-3OMPKJK48UX",
    "DAYI-PREMIUM-2FDSEREUWQ",
    "q",
    -- Buraya yeni key'leri ekleyebilirsin
}

local DISCORD_LINK = "https://discord.gg/KnSX3pfwP9"

-- ===================== SES =====================
local sound = Instance.new("Sound")
sound.SoundId = "rbxassetid://17584863692"  -- Gojo sesi
sound.Volume = 0.85
sound.Parent = game:GetService("SoundService")
sound:Play()

print("👋 Welcome back... Long time no see.")

-- ===================== UI =====================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "DayiKeySystem"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = playerGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 360, 0, 460)
MainFrame.Position = UDim2.new(0.5, -180, 0.5, -230)
MainFrame.BackgroundColor3 = Color3.fromRGB(14, 14, 18)
MainFrame.BorderSizePixel = 0
MainFrame.BackgroundTransparency = 1
MainFrame.Parent = ScreenGui

Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 16)

local Stroke = Instance.new("UIStroke", MainFrame)
Stroke.Color = Color3.fromRGB(170, 0, 255)
Stroke.Thickness = 2
Stroke.Transparency = 1

-- Close Button (Sağ Üst)
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 40, 0, 40)
CloseButton.Position = UDim2.new(1, -45, 0, 5)
CloseButton.BackgroundTransparency = 1
CloseButton.Text = "✕"
CloseButton.TextColor3 = Color3.fromRGB(255, 60, 60)
CloseButton.TextSize = 34
CloseButton.Font = Enum.Font.GothamBold
CloseButton.ZIndex = 10
CloseButton.Parent = MainFrame

-- Title ve diğer UI elemanları (aynı)
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1,0,0,90)
Title.Position = UDim2.new(0,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "DAYI\nKEY SYSTEM"
Title.TextColor3 = Color3.new(1,1,1)
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local Subtitle = Instance.new("TextLabel")
Subtitle.Size = UDim2.new(1,0,0,30)
Subtitle.Position = UDim2.new(0,0,0,125)
Subtitle.BackgroundTransparency = 1
Subtitle.Text = "Weekly Authentication"
Subtitle.TextColor3 = Color3.fromRGB(160,160,180)
Subtitle.TextScaled = true
Subtitle.Font = Enum.Font.GothamSemibold
Subtitle.Parent = MainFrame

local KeyBox = Instance.new("TextBox")
KeyBox.Size = UDim2.new(0.85,0,0,52)
KeyBox.Position = UDim2.new(0.075,0,0,170)
KeyBox.PlaceholderText = "Enter your key here..."
KeyBox.BackgroundColor3 = Color3.fromRGB(25,25,32)
KeyBox.TextColor3 = Color3.new(1,1,1)
KeyBox.TextScaled = true
KeyBox.Font = Enum.Font.GothamSemibold
KeyBox.Parent = MainFrame
Instance.new("UICorner", KeyBox).CornerRadius = UDim.new(0,12)

local VerifyButton = Instance.new("TextButton")
VerifyButton.Size = UDim2.new(0.85,0,0,55)
VerifyButton.Position = UDim2.new(0.075,0,0,235)
VerifyButton.BackgroundColor3 = Color3.fromRGB(130,0,255)
VerifyButton.Text = "VERIFY KEY"
VerifyButton.TextColor3 = Color3.new(1,1,1)
VerifyButton.TextScaled = true
VerifyButton.Font = Enum.Font.GothamBold
VerifyButton.Parent = MainFrame
Instance.new("UICorner", VerifyButton).CornerRadius = UDim.new(0,12)

local GetKeyButton = Instance.new("TextButton")
GetKeyButton.Size = UDim2.new(0.85,0,0,48)
GetKeyButton.Position = UDim2.new(0.075,0,0,300)
GetKeyButton.BackgroundColor3 = Color3.fromRGB(40,40,50)
GetKeyButton.Text = "GET KEY"
GetKeyButton.TextColor3 = Color3.fromRGB(180, 0, 255)
GetKeyButton.TextScaled = true
GetKeyButton.Font = Enum.Font.GothamSemibold
GetKeyButton.Parent = MainFrame
Instance.new("UICorner", GetKeyButton).CornerRadius = UDim.new(0,12)

local Status = Instance.new("TextLabel")
Status.Size = UDim2.new(1,0,0,26)
Status.Position = UDim2.new(0,0,1,-35)
Status.BackgroundTransparency = 1
Status.Text = "Ready"
Status.TextColor3 = Color3.fromRGB(180,180,190)
Status.TextScaled = true
Status.Font = Enum.Font.GothamSemibold
Status.Parent = MainFrame

-- ===================== ANIMATIONS =====================
local function openAnimation()
    MainFrame.Size = UDim2.new(0, 300, 0, 380)
    TweenService:Create(MainFrame, TweenInfo.new(0.55, Enum.EasingStyle.Quint), {
        Size = UDim2.new(0, 360, 0, 460),
        BackgroundTransparency = 0
    }):Play()
    TweenService:Create(Stroke, TweenInfo.new(0.6), {Transparency = 0}):Play()
end

local function successClose()
    VerifyButton.Text = "✓ ACCESS GRANTED"
    VerifyButton.BackgroundColor3 = Color3.fromRGB(0, 220, 100)
    
    local tween = TweenService:Create(MainFrame, TweenInfo.new(0.6, Enum.EasingStyle.Quint), {
        Size = UDim2.new(0, 300, 0, 380),
        BackgroundTransparency = 1
    })
    TweenService:Create(Stroke, TweenInfo.new(0.6), {Transparency = 1}):Play()
    tween:Play()
    
    tween.Completed:Connect(function()
        ScreenGui:Destroy()
        print("✅ Key Verified! Loading Main Hub...")
        loadstring(game:HttpGet("https://raw.githubusercontent.com/SCking31/dayi-hub/refs/heads/main/dayihub.lua"))()
    end)
end

-- ===================== CLOSE CONFIRMATION =====================
local function createConfirmation()
    local ConfirmFrame = Instance.new("Frame")
    ConfirmFrame.Size = UDim2.new(0, 300, 0, 180)
    ConfirmFrame.Position = UDim2.new(0.5, -150, 0.5, -90)
    ConfirmFrame.BackgroundColor3 = Color3.fromRGB(18, 18, 22)
    ConfirmFrame.ZIndex = 20
    ConfirmFrame.Parent = ScreenGui
    Instance.new("UICorner", ConfirmFrame).CornerRadius = UDim.new(0, 16)

    local TitleConfirm = Instance.new("TextLabel")
    TitleConfirm.Size = UDim2.new(1,0,0,60)
    TitleConfirm.BackgroundTransparency = 1
    TitleConfirm.Text = "Do you wanna close this page?"
    TitleConfirm.TextColor3 = Color3.new(1,1,1)
    TitleConfirm.TextScaled = true
    TitleConfirm.Font = Enum.Font.GothamSemibold
    TitleConfirm.ZIndex = 21
    TitleConfirm.Parent = ConfirmFrame

    local CancelBtn = Instance.new("TextButton")
    CancelBtn.Size = UDim2.new(0.45,0,0,50)
    CancelBtn.Position = UDim2.new(0.05,0,0.6,0)
    CancelBtn.BackgroundColor3 = Color3.fromRGB(70,70,80)
    CancelBtn.Text = "Cancel"
    CancelBtn.TextColor3 = Color3.new(1,1,1)
    CancelBtn.TextScaled = true
    CancelBtn.Font = Enum.Font.GothamSemibold
    CancelBtn.ZIndex = 21
    CancelBtn.Parent = ConfirmFrame
    Instance.new("UICorner", CancelBtn).CornerRadius = UDim.new(0,12)

    local YesBtn = Instance.new("TextButton")
    YesBtn.Size = UDim2.new(0.45,0,0,50)
    YesBtn.Position = UDim2.new(0.5,0,0.6,0)
    YesBtn.BackgroundColor3 = Color3.fromRGB(200, 40, 40)
    YesBtn.Text = "Yes"
    YesBtn.TextColor3 = Color3.new(1,1,1)
    YesBtn.TextScaled = true
    YesBtn.Font = Enum.Font.GothamBold
    YesBtn.ZIndex = 21
    YesBtn.Parent = ConfirmFrame
    Instance.new("UICorner", YesBtn).CornerRadius = UDim.new(0,12)

    CancelBtn.MouseButton1Click:Connect(function()
        ConfirmFrame:Destroy()
    end)

    YesBtn.MouseButton1Click:Connect(function()
        ScreenGui:Destroy()
        print("❌ Key System Closed.")
    end)
end

CloseButton.MouseButton1Click:Connect(createConfirmation)

-- ===================== BUTTONS =====================
VerifyButton.MouseButton1Click:Connect(function()
    local entered = KeyBox.Text:upper():gsub("%s+", "")
    if entered == "" then return end
    
    Status.Text = "Checking..."
    task.wait(0.9)
    
    if table.find(validKeys, entered) then
        successClose()
    else
        Status.Text = "Invalid Key"
        Status.TextColor3 = Color3.fromRGB(255, 70, 70)
        task.wait(1.4)
        Status.Text = "Ready"
        Status.TextColor3 = Color3.fromRGB(180,180,190)
    end
end)

GetKeyButton.MouseButton1Click:Connect(function()
    setclipboard(DISCORD_LINK)
    GetKeyButton.Text = "COPIED TO CLIPBOARD!"
    GetKeyButton.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
    task.wait(2)
    GetKeyButton.Text = "GET KEY"
    GetKeyButton.BackgroundColor3 = Color3.fromRGB(40,40,50)
end)

KeyBox.FocusLost:Connect(function(enterPressed)
    if enterPressed then VerifyButton.MouseButton1Click:Fire() end
end)

openAnimation()
print("✅ DAYI Key System Loaded!")
